import 'package:flutter/material.dart';

class SportsScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sports Screen'),
      ),
      body: Center(
        child: Text('Sports Screen Content'),
      ),
    );
  }
}
